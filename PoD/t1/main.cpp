#include "ordenacao.hpp"

int main (void) {
  externalSort("teste.txt");

  // ifstream f;
  // f.open("testando.txt");
  // char c,a,b;
  // f.get(a);
  // f.get(b);
  // f.get(c);
  //
  // cout << "A = " << a << endl;
  // cout << "B = " << b << endl;
  // cout << "C = " << c << endl;
  //
  //
  // if (f.eof()) {
  //   cout << "\n\nHAHA";
  // }

  return 0;
}
